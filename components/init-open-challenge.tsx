"use client"

// This component is kept for backward compatibility
// but it doesn't do anything anymore since we've changed our approach
export function InitOpenChallenge() {
  // No initialization needed anymore
  return null
}
